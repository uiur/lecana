= form_for [subject, upload], :html => {:multipart => true} do |f|
  .file_row
    = f.label :name, 'ファイル'
    = f.file_field :file, :class => 'file_input_selectfile'
  .file_row
    = f.label :name, '名前'
    = f.text_field :name, :class => 'file_input_name'
  .file_notice
    %p 容量30MB以下のファイルをアップロードできます。ファイルの拡張子は、.pdf, .txt, .jpg, .gif, .png です。
  = f.submit 'アップロード', :class => 'file_button_upload'
